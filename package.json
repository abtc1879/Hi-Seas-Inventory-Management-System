{
  "name": "hiseas-inventory-management",
  "version": "1.0.0",
  "description": "Material Inventory Management System with Firebase Realtime Database",
  "scripts": {
    "init-db": "node init-rtdb.js",
    "deploy:rules": "firebase deploy --only database",
    "deploy:all": "firebase deploy",
    "serve": "firebase serve"
  },
  "devDependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-tools": "^12.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
